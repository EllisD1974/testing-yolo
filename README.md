# testing-yolo
Testing the YOLO model


# Get the testing official yolo dataset EX
Run ```python download_coco128.py``` to download the data set and prepare it by renaming files as needed.

To train run ```python train.py```
To test using the model ```python run_inference.py```
